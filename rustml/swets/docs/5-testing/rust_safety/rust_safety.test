# Test Plan: Rust Safety

**Version:** 1.0
**Status:** Draft
**Spec:** [Feature Spec](../1-requirements/rust_safety/rust_safety.spec)

## Test Cases

| ID | Test | Verifies | Priority |
|----|------|----------|----------|
| TC-001 | NFR-300: No `unsafe` code outside SIMD kernels (Analysis) | REQ-001 | Must |
| TC-002 | NFR-301: `Storage::View` holds `Arc<Storage>` (not `Arc<Tensor>`) to prevent reference cycles (Analysis) | REQ-002 | Must |
| TC-003 | NFR-302: `GradientTape` borrow semantics must compile — design must resolve `Option<&mut GradientTape>` threading through nested layer calls (see Section 5.1) (Analysis) | REQ-003 | Must |
| TC-004 | NFR-303: All `BackwardOp` implementations must be `Send + Sync` (Analysis) | REQ-004 | Must |

